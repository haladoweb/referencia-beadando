import { BuildingSearchPipe } from './building-search-pipe';

describe('BuildingSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildingSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
